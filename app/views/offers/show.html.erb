<div class="banner pt-2 pb-2" style="background-image: linear-gradient(rgba(255,100,0,1),rgba(255,100,0,1))">
  <div class="container">
    <h1> Rent <%= @offer.game.name %> from <%= @offer.user.username %> </h1>
    <p> <%= @offer.comment %> </p>
      <%= link_to "All games", games_path, class: "btn btn-primary" %>
  </div>
</div>
<div class="container">
</div>

<%= render 'bookings/form' %>



<!-- <%#= link_to "Book now!", create_offer_booking_path %>  -->
